# Sample environment variables for local testing / vscode launch
# Required by lambda_handler.py
BUCKET_NAME=dummy-bucket
BASE_PREFIX=base/prefix
TIMEZONE=America/New_York

# Used by test.py to filter which tables to exercise
TABLE_PREFIX=dev_tables

# Local-output stubbing (test/test runner): set LOCAL_OUTPUT=1 to write locally under LOCAL_OUTPUT_DIR
LOCAL_OUTPUT=1
LOCAL_OUTPUT_DIR=./output

# AWS region (optional if your boto3 session already has one)
AWS_REGION=us-east-1

# Optional: supply credentials for local runs (DO NOT commit real credentials)
# AWS_PROFILE=your-aws-profile
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...
